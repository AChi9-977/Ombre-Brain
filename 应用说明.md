# Ombre Brain 省 Token / 检索优化覆盖包

## 这是什么

这是基于官方公开版 2.7.0 制作的“增量覆盖包”，只包含本次修改的程序、配置示例、说明和回归测试。它不是完整仓库，不包含也不会删除你的自定义模板。

已完成的功能：

- catalog 一桶一行显示名称、主题、重要度、ID、情感坐标和更新时间，不返回正文。
- breath 普通搜索结果默认 5 个；命中的钉选、保护、固化桶不占名额；末尾提示还有多少相关桶未显示。
- 精确字段、中文原词和正文原词严格排在纯语义结果之前；显式搜索不再混入随机记忆。
- breath_search / breath_advanced 支持 `date_from`、`date_to`，使用不会被搜索刷新影响的 `updated_at`。
- pulse 默认显示全部核心桶和权重最高的 15 个普通桶，统计放在末尾；`show_all=true` 查看全部。
- dream 默认最近 5 个摘要；`detail_ids="ID1,ID2"` 返回指定全文；`max_tokens` 控制辅助区块预算。
- trace 支持逗号分隔的最多 50 个 ID 批量修改安全字段；任一 ID 不存在会取消整批。
- trace 支持 `merge=目标ID` 合并普通动态桶；源桶只归档、不物理删除，钉选/保护/固化桶禁止合并。

未实施：重复的语义索引建设、todos 新工具、dormant 状态及未定义数据结构的 related 功能。

## 应用方法

1. 先备份当前项目，特别是你新增的两个模板和 Railway 持久卷数据。
2. 解压 `Ombre-Brain-token-search-upgrade-overlay.zip`。
3. 将解压后的内容复制到你现有仓库根目录，选择“替换同名文件”。不要先清空仓库。
4. 如果现有 `config.yaml` 里仍是 `surfacing.breath_max_results: 20`，把它改成 `5`；覆盖包只修改 `config.example.yaml`，不会擅自改你的实际配置。
5. 提交并推送到 GitHub 后，Railway 会按原项目连接自动部署；部署后检查 `/health` 返回 200。

你目前登不上 GitHub时，可以先保留这个覆盖包，等账号恢复后再执行第 3～5 步。没有 GitHub 写入权限时，我无法替你把本地改动直接发布到 Railway。

## 重要提醒

- 覆盖包不包含模板目录，因此不会主动改动两个自定义模板。
- 不要用“先删除仓库内容再上传”的方式，否则未包含在覆盖包中的模板会丢失。
- 以后点击软件内“更新到官方 main”可能覆盖这些自定义优化。建议先把优化提交到你自己的分支；升级官方版本时重新合并或重新应用覆盖包。
- 合并记忆前仍建议先备份；实现会归档源桶，但批量写操作本身不可视为备份。

## Railway 启动时报 `atomic_write_text` 导入错误

这表示仓库处于混合版本：`bucket_manager.py` 已更新，但 `utils.py` 仍是旧版。2026-07-15 修正版覆盖包已经在 `bucket_manager.py` 内加入兼容实现，不再要求旧仓库提供这个函数。

处理方法：只需把修正版覆盖包里的 `src/bucket_manager.py` 再次上传并覆盖 GitHub 仓库中的同路径文件，提交后等待 Railway 自动重新部署。不要把文件放到仓库根目录；必须保持路径为 `src/bucket_manager.py`。

## 验证结果

- 完整自动测试：1130 项通过。
- 45 项因需要 Docker、网络或特定外部环境而按测试条件跳过。
- 2 条提示来自测试故意生成重复 ZIP 文件名，用于验证更新包安全检查，不是本次功能错误。
